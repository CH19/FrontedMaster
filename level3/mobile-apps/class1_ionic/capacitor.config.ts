import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'class1_ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
